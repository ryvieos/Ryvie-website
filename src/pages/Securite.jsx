import React from 'react';
import { Shield, Lock, Wifi, Eye, Brain, CheckCircle } from 'lucide-react';
import Security from '../components/Security';

const Securite = () => {
  return (
    <div className="pt-20">
      <Security />
    </div>
  );
};

export default Securite;
